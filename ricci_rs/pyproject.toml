[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "ricci_rs"
version = "0.1.0"
description = "Rust-accelerated Ollivier-Ricci curvature + Wasserstein-1 LP solver"

[tool.maturin]
module-name = "ricci_rs"
